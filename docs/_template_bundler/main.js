import "crisp-game-lib";

const title = "";

const description = `
`;

const characters = [];

init({
  update,
  title,
  description,
  characters,
  options: {},
});

function update() {
  if (!ticks) {
  }
}
